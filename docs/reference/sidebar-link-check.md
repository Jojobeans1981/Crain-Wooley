# Sidebar link check

Validated 79 distinct internal sidebar links against the clone route tree (legacy catch-all paths + app routes + redirects). External / tel: / mailto: skipped. **Flagged only — not rewritten** (DECISION 2).

## All sidebar links resolve to a clone route ✅
